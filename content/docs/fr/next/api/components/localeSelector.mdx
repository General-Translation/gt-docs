---
title: <LocaleSelector>
description: Référence API pour le composant <LocaleSelector>
---

## Vue d'ensemble

Le composant `<LocaleSelector>` est utilisé pour sélectionner la langue de l'utilisateur.
C'est un composant côté client qui fournit un menu déroulant pour sélectionner la langue.

## Référence

### Retours

Un composant qui permet à l'utilisateur de sélectionner sa langue.

---

## Exemples

### Utilisation de base

```jsx
"use client";
import { LocaleSelector } from 'gt-next/client';

export default function MyComponent() {
    return (
        <LocaleSelector />
    );
}
```

---

## Notes
* Le composant `<LocaleSelector>` vous permet de sélectionner une langue différente pour votre application.
* Le composant `<LocaleSelector>` n'est pas disponible dans le composant serveur.

## Prochaines étapes

* En savoir plus sur le hook [`useLocale()`](/docs/next/api/helpers/useLocale).
* Consultez le hook [`useLocaleSelector()`](/docs/next/api/helpers/useLocaleSelector) pour définir un sélecteur de locale personnalisé.
* En savoir plus sur les chaînes de locale [ici](/docs/reference/locale-strings).
