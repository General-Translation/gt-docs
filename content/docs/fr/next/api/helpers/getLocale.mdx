---
title: getLocale()
description: Référence API pour la méthode côté serveur getLocale
---

## Aperçu

La fonction `getLocale()` récupère la locale actuelle de l'utilisateur lors du rendu côté serveur.
La locale retournée est [formatée en tant que chaîne](/docs/reference/locale-strings), par exemple, `'en-US'`.

<Callout>
    `getLocale()` est une méthode côté serveur et ne peut être utilisée que sur des composants côté serveur.
</Callout>

Pour une utilisation côté client, voir [`useLocale()`](/docs/next/api/helpers/useLocale).

## Référence

### Retours

Une promesse qui se résout en une chaîne représentant la locale actuelle de l'utilisateur, par exemple, `'en-US'`.

---

## Comportement de repli
Lorsqu'une locale non prise en charge est demandée, une locale de repli sera sélectionnée.

Par exemple, dans le cas d'une locale non prise en charge,
si (1) l'utilisateur a configuré plusieurs locales préférées dans les paramètres de son navigateur,
et (2) l'une de ces locales est prise en charge par votre application,
alors la locale se repliera sur la meilleure langue.

De plus, si aucune locale de repli possible n'est disponible,
mais que deux locales partagent la même langue (par exemple, `en-US` et `en-GB`),
alors la locale se repliera sur la locale prise en charge qui partage la même langue.

Si aucune des conditions ne peut être remplie, alors la locale par défaut sera utilisée.

Voir [`le guide de gestion des locales()`](/docs/next/reference/locale-management) pour des informations sur la configuration des locales prises en charge.

---

## Exemples

### Utilisation de base

Récupérer la locale de l'utilisateur lors du rendu côté serveur.

```javascript title="GetUserLocale.jsx" copy
import getLocale from 'gt-next/server';

export default async function GetUserLocale() {
    const locale = await getLocale(); // [!code highlight]
    return <p>User locale: {locale}</p>;
}
```

---


## Notes
 * La fonction `getLocale()` est asynchrone et doit être attendue pour récupérer la locale.
 * Elle est spécifiquement conçue pour une utilisation côté serveur. Voir [`useLocale()`](/docs/next/api/helpers/useLocale) pour les composants côté client.
 * La locale retournée respecte le format des [chaînes de locale](/docs/reference/locale-strings).

## Prochaines étapes
 * Apprenez à configurer les locales prises en charge avec [Configuration i18n](/docs/next/reference/i18n-configuration).
 * Pour gérer les locales de secours, voir [`getDefaultLocale()`](/docs/next/api/helpers/getDefaultLocale).