---
title: <LocaleSelector>
description: <LocaleSelector>コンポーネントのAPIリファレンス
---

## 概要

`<LocaleSelector>` コンポーネントは、ユーザーのロケールを選択するために使用されます。
これは、ロケールを選択するためのドロップダウンを提供するクライアントサイドのコンポーネントです。

## リファレンス

### 戻り値

ユーザーが自分のロケールを選択できるコンポーネント。

---

## 例

### 基本的な使用法

```jsx
"use client";
import { LocaleSelector } from 'gt-next/client';

export default function MyComponent() {
    return (
        <LocaleSelector />
    );
}
```

---

## メモ
* `<LocaleSelector>` コンポーネントを使用すると、アプリのロケールを変更できます。
* `<LocaleSelector>` コンポーネントはサーバーコンポーネントでは利用できません。

## 次のステップ

* [`useLocale()`](/docs/next/api/helpers/useLocale) フックについてもっと学びましょう。
* カスタムロケールセレクターを定義するための [`useLocaleSelector()`](/docs/next/api/helpers/useLocaleSelector) フックをチェックしてください。
* ロケール文字列についてもっと学ぶには[こちら](/docs/reference/locale-strings)を参照してください。
